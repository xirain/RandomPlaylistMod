
# Beat Saber Random Playlist Mod - 设计方案

## 1. 项目概述

本模组旨在为Beat Saber游戏提供一个智能的随机歌曲播放功能，允许用户设定特定时长并从多个播放列表中随机选择歌曲进行连续播放。

### 1.1 核心功能

| 功能模块 | 描述 |
|---------|------|
| 时长设置 | 允许用户自定义游戏时长（30分钟、1小时等） |
| 播放列表选择 | 允许用户勾选多个目标播放列表 |
| 智能随机算法 | 在设定时长内合理分配歌曲，避免重复 |
| 无缝歌曲切换 | 保证游戏体验的连贯性 |

### 1.2 技术栈

| 依赖 | 版本 | 用途 |
|-----|------|------|
| BSIPA | 4.1.4+ | 模组框架 |
| SongCore | 3.9.3+ | 歌曲数据管理 |
| SiraUtil | 3.0.0+ | 核心工具库 |
| BeatSaberMarkupLanguage | 1.6.3+ | UI构建 |
| PlaylistCore | 1.1.0+ | 播放列表管理 |

---

## 2. 系统架构设计

### 2.1 架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户界面层                               │
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────────────┐    │
│  │ 时长设置面板 │  │ 播放列表选择 │  │    开始播放按钮     │    │
│  └──────┬───────┘  └──────┬───────┘  └──────────┬──────────┘    │
└─────────┼──────────────────┼──────────────────────┼──────────────┘
          │                  │                      │
          ▼                  ▼                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                        业务逻辑层                               │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                  PlaySessionManager                     │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │    │
│  │  │ PlaylistMgr  │  │ SongSelector │  │ TimeManager  │  │    │
│  │  └──────────────┘  └──────────────┘  └──────────────┘  │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
          │                  │                      │
          ▼                  ▼                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                        数据访问层                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ SongCore API │  │PlaylistCore  │  │ 游戏内置API  │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 核心模块划分

#### 2.2.1 PlaySessionManager（播放会话管理器）

**职责**：管理整个播放会话的生命周期

| 方法 | 功能 |
|-----|------|
| StartSession() | 启动播放会话 |
| EndSession() | 结束播放会话 |
| OnSongCompleted() | 处理歌曲完成事件 |
| GetNextSong() | 获取下一首歌曲 |

#### 2.2.2 PlaylistManager（播放列表管理器）

**职责**：管理播放列表的加载和选择

| 方法 | 功能 |
|-----|------|
| LoadPlaylists() | 加载所有可用播放列表 |
| GetSelectedPlaylists() | 获取用户选择的播放列表 |
| GetSongsFromPlaylists() | 从选中的播放列表获取所有歌曲 |

#### 2.2.3 SongSelector（歌曲选择器）

**职责**：实现智能随机歌曲选择算法

| 方法 | 功能 |
|-----|------|
| GenerateSongQueue() | 根据时长生成歌曲队列 |
| SelectNextSong() | 选择下一首歌曲 |
| ShuffleSongs() | 打乱歌曲顺序 |

#### 2.2.4 TimeManager（时间管理器）

**职责**：管理播放时长和倒计时

| 方法 | 功能 |
|-----|------|
| SetDuration() | 设置播放时长 |
| GetRemainingTime() | 获取剩余时间 |
| IsSessionComplete() | 判断会话是否结束 |

---

## 3. 用户界面设计

### 3.1 界面布局草图

```
┌──────────────────────────────────────────────────────────────┐
│                    Random Playlist Mod                       │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              播放时长设置                             │   │
│  │                                                      │   │
│  │  [ 30分钟 ] [ 1小时 ] [ 2小时 ] [ 自定义: ____ ]     │   │
│  │                                                      │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              播放列表选择                             │   │
│  │                                                      │   │
│  │  ☑ Playlist A          ☑ Playlist C                  │   │
│  │  ☑ Playlist B          ☐ Playlist D                  │   │
│  │                                                      │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  预计歌曲数: XX首  |  预计时长: XX分XX秒             │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│                    [     开始播放     ]                      │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 3.2 UI组件设计

| 组件 | 类型 | 功能 |
|-----|------|------|
| DurationButtons | ButtonGroup | 预设时长按钮（30分钟、1小时、2小时） |
| CustomDurationInput | InputField | 自定义时长输入框 |
| PlaylistCheckboxes | ToggleGroup | 播放列表多选框 |
| SongCountLabel | Text | 显示预计歌曲数量 |
| TimeEstimateLabel | Text | 显示预计总时长 |
| StartButton | Button | 开始播放按钮 |

---

## 4. 数据流程图

```
用户设置时长 ──────────────┐
                          │
用户选择播放列表 ──────────┼──▶ PlaySessionManager
                          │         │
                          ▼         ▼
                     LoadPlaylists()
                          │
                          ▼
                  GetSongsFromPlaylists()
                          │
                          ▼
              SongSelector.GenerateSongQueue()
                          │
                          ▼
                     开始播放第一首
                          │
              ┌───────────┴───────────┐
              ▼                       ▼
         歌曲播放中              检查剩余时间
              │                       │
              ▼                       ▼
         歌曲完成              时间不足?
              │                       │
              │                       ├── 是 ──▶ 结束会话
              │                       │
              ▼                       ▼
         获取下一首歌曲           继续播放
              │
              ▼
         加载并播放歌曲
```

---

## 5. 关键算法设计

### 5.1 智能随机歌曲选择算法

#### 算法流程：

1. **收集歌曲池**：从所有选中的播放列表中收集歌曲
2. **过滤可用歌曲**：排除已播放过的歌曲和不可用的歌曲
3. **时长预估**：计算每首歌曲的平均时长，估算需要的歌曲数量
4. **智能选择**：
   - 优先选择时长接近剩余时间的歌曲（当剩余时间较少时）
   - 确保歌曲多样性，避免连续播放同一位作者的歌曲
   - 使用Fisher-Yates洗牌算法确保随机性

#### 伪代码：

```csharp
List<Song> GenerateSongQueue(List<Playlist> selectedPlaylists, int targetDurationMinutes)
{
    // 1. 收集所有歌曲
    List<Song> allSongs = selectedPlaylists.SelectMany(p => p.Songs).ToList();
    
    // 2. 打乱顺序
    Shuffle(allSongs);
    
    // 3. 计算目标秒数和平均歌曲时长
    int targetSeconds = targetDurationMinutes * 60;
    double avgDuration = allSongs.Average(s => s.Duration);
    
    // 4. 估算需要的歌曲数量
    int estimatedCount = (int)(targetSeconds / avgDuration);
    
    // 5. 智能选择歌曲
    List<Song> queue = new List<Song>();
    int currentTotal = 0;
    string lastAuthor = "";
    
    foreach (Song song in allSongs)
    {
        // 跳过与上一首同作者的歌曲
        if (song.Author == lastAuthor) continue;
        
        // 如果加入这首歌会超出目标时长太多，跳过
        if (currentTotal + song.Duration > targetSeconds + 120) continue;
        
        queue.Add(song);
        currentTotal += song.Duration;
        lastAuthor = song.Author;
        
        // 达到目标时长或歌曲数量限制
        if (currentTotal >= targetSeconds || queue.Count >= estimatedCount + 2) break;
    }
    
    return queue;
}

void Shuffle<T>(List<T> list)
{
    // Fisher-Yates 洗牌算法
    Random rng = new Random();
    int n = list.Count;
    while (n > 1)
    {
        n--;
        int k = rng.Next(n + 1);
        T value = list[k];
        list[k] = list[n];
        list[n] = value;
    }
}
```

### 5.2 时长计算逻辑

| 场景 | 处理方式 |
|-----|------|
| 剩余时间 > 最长歌曲时长 | 随机选择下一首歌曲 |
| 剩余时间 <= 最长歌曲时长 | 选择时长最接近剩余时间的歌曲 |
| 剩余时间 < 最短歌曲时长 | 播放最后一首可用歌曲后结束 |

---

## 6. 错误处理机制

### 6.1 错误类型与处理

| 错误类型 | 触发条件 | 处理方式 |
|---------|---------|---------|
| 无可用播放列表 | 用户未选择任何播放列表 | 显示警告并禁用开始按钮 |
| 播放列表为空 | 选中的播放列表中没有歌曲 | 显示警告并建议选择其他播放列表 |
| 歌曲加载失败 | 歌曲文件损坏或不存在 | 跳过该歌曲，播放下一首 |
| 时长设置无效 | 自定义时长小于1分钟或大于12小时 | 显示错误提示 |
| 会话意外中断 | 游戏崩溃或用户退出 | 保存当前进度，下次可继续 |

### 6.2 日志记录

```csharp
// 日志级别
- Info: 正常操作（如会话开始、歌曲切换）
- Warning: 非致命问题（如跳过损坏的歌曲）
- Error: 致命错误（如无法加载播放列表）
- Debug: 调试信息（开发模式下）
```

---

## 7. 性能优化策略

### 7.1 内存优化

| 策略 | 说明 |
|-----|------|
| 延迟加载 | 仅在需要时加载歌曲详情 |
| 对象池 | 复用歌曲对象，减少GC压力 |
| 异步操作 | 在后台线程执行耗时操作 |

### 7.2 加载优化

| 策略 | 说明 |
|-----|------|
| 预加载 | 在播放前预加载下一首歌曲 |
| 缓存机制 | 缓存已加载的播放列表信息 |
| 并行加载 | 同时加载多个播放列表 |

---

## 8. 兼容性考虑

### 8.1 游戏版本支持

- 目标版本：Beat Saber 1.29.4+
- 向下兼容：支持1.25.0+版本

### 8.2 模组兼容性

| 依赖模组 | 最小版本 | 说明 |
|---------|---------|------|
| BSIPA | 4.1.4 | 模组加载框架 |
| SongCore | 3.9.3 | 歌曲数据管理 |
| SiraUtil | 3.0.0 | 核心工具库 |
| BeatSaberMarkupLanguage | 1.6.3 | UI框架 |
| PlaylistCore | 1.1.0 | 播放列表支持（可选） |

---

## 9. 部署与安装

### 9.1 文件结构

```
RandomPlaylistMod/
├── RandomPlaylistMod.dll          # 主程序集
├── RandomPlaylistMod.pdb          # 调试符号（可选）
├── manifest.json                  # 模组元数据
└── README.md                      # 使用说明
```

### 9.2 安装步骤

1. 确保已安装BSIPA和所有依赖模组
2. 将`RandomPlaylistMod.dll`复制到`Beat Saber/Plugins`目录
3. 启动游戏，模组会自动加载

---

## 10. 测试计划

### 10.1 单元测试

| 测试项 | 测试内容 | 预期结果 |
|-------|---------|---------|
| 时长计算 | 测试不同时长设置 | 正确计算歌曲数量 |
| 随机算法 | 多次运行验证随机性 | 每次结果不同，无明显规律 |
| 播放列表加载 | 加载空列表和正常列表 | 正确处理边界情况 |
| 歌曲过滤 | 测试同作者过滤 | 不会连续播放同作者歌曲 |

### 10.2 集成测试

| 测试项 | 测试内容 | 预期结果 |
|-------|---------|---------|
| 完整流程 | 从设置到播放结束 | 流畅完成整个会话 |
| 歌曲切换 | 测试无缝切换 | 无明显卡顿或延迟 |
| 错误恢复 | 模拟歌曲加载失败 | 自动跳过并继续播放 |
| 性能测试 | 长时间运行 | 内存稳定，无内存泄漏 |

---

## 附录：核心数据模型

### Song（歌曲）

| 属性 | 类型 | 说明 |
|-----|------|------|
| SongName | string | 歌曲名称 |
| Author | string | 作者名称 |
| Duration | int | 时长（秒） |
| Key | string | 歌曲唯一标识 |
| PlaylistName | string | 所属播放列表 |

### Playlist（播放列表）

| 属性 | 类型 | 说明 |
|-----|------|------|
| Name | string | 播放列表名称 |
| Songs | List\<Song\> | 歌曲列表 |
| IsSelected | bool | 是否被选中 |

### PlaySession（播放会话）

| 属性 | 类型 | 说明 |
|-----|------|------|
| TargetDuration | int | 目标时长（分钟） |
| SelectedPlaylists | List\<Playlist\> | 选中的播放列表 |
| SongQueue | Queue\<Song\> | 歌曲队列 |
| CurrentSong | Song | 当前播放的歌曲 |
| ElapsedTime | int | 已播放时间（秒） |
| IsActive | bool | 会话是否活跃 |
